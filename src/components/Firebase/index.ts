import FirebaseContext, { withFirebase, FirebaseContextInterface } from './context';
import Firebase from './firebase';

export default Firebase;

export { FirebaseContext, withFirebase, FirebaseContextInterface };